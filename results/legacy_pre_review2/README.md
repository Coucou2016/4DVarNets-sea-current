# Legacy GPU96 results (pre-review2 / pre_p0_fix)

These crop_size=96, ~20-epoch NATL60 scores predate Round-2 Stage A correctness fixes
(geometry maps, full unroll graph, paper-mode obs/oi, OI-only geo baseline, etc.).

**Do not cite as paper Table rows.** Historical directional note only: B2 > M3 ≳ M4 on τ_uv.

Tag: `legacy_pre_review2` / `pre_p0_fix`.
Conceptual release after Stage A: `v0.2.0-review2-fixed`.
